const data = require('./test.json');

function getGammaRate(data) {
  const indexes = data[0].length;
  const gammaGrid = [];
  data.forEach((row, i) => {
    row.split('').forEach((value, i) => {
      gammaGrid[i]
    });
  });

  grid.map(row => {

  });

}

getGammaRate(data)